import React from 'react';

import cls from "./presentationPreview.module.sass"

const PresentationPreview = () => {
    return (
        <div className={cls.present}>

        </div>
    );
};

export default PresentationPreview;